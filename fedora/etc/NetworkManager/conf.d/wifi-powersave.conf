# Disable Wi-Fi power save to stop real-time stutter (Google Meet, calls).
# Power save lets the NIC nap between packets, adding latency spikes and
# dropped frames on bursty audio/video despite an otherwise healthy link.
# wifi.powersave: 2 = disabled, 3 = enabled.
[connection]
wifi.powersave = 2
