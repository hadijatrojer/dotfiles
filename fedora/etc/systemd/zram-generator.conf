[zram0]
# Logical capacity; physical RAM is allocated only as compressed pages are used.
zram-size = ram

# Preserve at least half of physical RAM for the active working set, even when
# swapped pages compress poorly.
zram-resident-limit = ram / 2
compression-algorithm = zstd
swap-priority = 100
