# Flatpak setup

## Per-app setup

- [`cider/`](cider/README.md) — install, manual upgrade, and URL-handler setup
  for the user-scoped Cider (`sh.cider.Cider`) Apple Music Flatpak bundle. Run
  `cider/setup-cider.sh` after installing or upgrading the downloaded bundle.
